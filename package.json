{
  "name": "briefs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static read-only reader for the Monday, Friday and Sunday briefs. No dependencies, no network calls.",
  "scripts": {
    "build": "node --no-warnings build.mjs",
    "publish": "node --no-warnings build.mjs && node publish.mjs",
    "offline": "node --no-warnings build.mjs --offline"
  }
}
